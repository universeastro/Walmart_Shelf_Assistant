# 项目需求文档 02 并行功能

## 目标

在不改变主线映射行为的前提下，为 `文件/02/` 增加独立支线流程：从 A 文件读取 `SKU` 与 `自定义`，写入 B 文件副本的 `SKU` 与 `平台SKU`。主线素材位于 `文件/01/`，支线素材位于 `文件/02/`，两套映射契约不得混用。

## 已确认需求

1. 支线源文件为 `文件/02/A模板02.xls`，目标文件为 `文件/02/B模板.xls`。
2. `A.A列“SKU” -> B.A列“SKU”`。
3. `A.D列“自定义” -> B.B列“平台SKU”`；真实业务选品表可能将同一字段命名为“自定义SKU”，支线应兼容这两个表头名称。
4. 优先按表头名定位字段；需求文档原先将“平台SKU”列标写成 A，现已更正为 B，与模板实测一致。
5. 支线默认追加到目标末尾并留出 3 个间隔行；真实 `B模板.xls` 末行为 R696，因此新批次从 R700 起。
   空目标从第 2 行写入。覆盖旧数据仅允许通过显式 `-WriteMode Replace` 启用；参考视频不能作为默认清空既有数据的依据。
6. A/B 支持路径选择或拖入窗口；按钮选择必须保留为后备入口。
7. 输出必须是 B 文件副本，只写值；不得修改原始 B 文件。
8. 主线与支线使用同一窗口内的独立页面；切换方案时切换页面并恢复该方案上次使用的 A、B、输出路径及导出范围。
9. 输出文件已存在时，由用户选择“替换现有输出”或“在现有输出中继续写入”；继续写入仍遵循末行后留 3 行的追加规则。

## 技术约束

- 继续使用 Excel COM 打开、写入和保存 B 副本；不得用 openpyxl/ExcelJS 保存 B。
- 支线必须使用独立 profile，目标表头识别和写入起始行不能复用沃尔玛多级模板假设。
- 显式替换支线目标数据区时使用 `.ClearContents()`，不得用 `.Value2 = $null`。
- 表头名称是首要依据；列字母仅可作为明确记录的兜底。
- PowerShell 二维数组下标中的减法必须加括号。
- B01 主线既有映射、JSON 摘要字段和验证行为保持兼容。
- `ShelfAssistant(settings_path=...)` 与 `_load_paths` 的字符串类型检查必须保留。
- 可选拖放依赖缺失时 GUI 仍须正常启动并可通过“浏览”完成流程。

## 验收标准

- 支线输出保留第 1 行表头；空目标从第 2 行写入，有既有记录时从末行 + 4 追加。
- `SKU` 与 `平台SKU` 同行对应；A 中被跳过的隐藏行/空行遵循所选 RowMode，并有摘要统计。
- 默认追加时既有数据逐格不变且保留 3 个空白间隔行；显式替换时才清理本次源数据范围外的旧值。原始 B 文件哈希不变。
- 目标列始终按 `SKU`、`平台SKU` 表头定位；缺失或歧义时停止并给出可读错误，不猜测相邻列。
- GUI 可明确选择主线/支线方案；支线只启用其适用的行模式和映射契约。
- 拖入 A/B/输出路径支持含空格路径、重复拖入覆盖；非法扩展/多文件拖入给出可读错误；运行中禁止改路径。
- 两个方案的路径与导出范围互不覆盖，重启后恢复最后页面和两组设置；旧版单组路径配置自动迁移且不丢失，缺少导出范围时默认仅可见行。
- 选择继续写入时，以现有输出文件为基底生成私有工作副本，成功后更新该输出；文件 A 与文件 B 原件均不得修改。
- B01 原验证、Python unittest、支线专用逐格验证、原模板哈希验证均通过并写入 `HANDOFF.md`。

## 分工

- Codex（主负责人）：实现 profile、GUI 接口、拖放、测试、修复、整合与提交。
- Claude（独立审查）：需求/验收审查、边界与回归检查、文档和使用流程检查；接口冲突先记入 `HANDOFF.md`。

## 当前状态

- 分支：`feature/req02-parallel`
- 基线：`68c2ad39 chore: 入库第二套模板 A/B模板02`
- 目录口径：`文件/01/` 为主线，`文件/02/` 为支线。
- C-01 已按用户此前明确答复生效：默认追加；显式 `Replace` 仅用于明确要求覆盖旧数据的场景。
- 流程文件 `docs/WORKFLOW.md` 已建立，补齐变更控制、升级、冻结窗口、验证独立性、回归门槛、
  共写规则、DoD 与素材基线。
- 用户于 2026-09-11 更新需求文档，明确目标“平台SKU”为 B 列；实现仍以表头名称定位，列字母仅作为需求核对依据。
