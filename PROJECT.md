# 项目需求文档 03 并行功能

## 目标

在不改变主线 01 与支线 02 既有行为的前提下，新增第三条并行流程：把文件 A 的
`自定义SKU`、`SKU价(￥)`、`重量(g)`、`长`、`宽`、`高` 六个字段，按表头名写入文件 B
`导入 单位转换` 工作表的 `SKU(直接从sheet 1导入）`、`SKU价(￥)`、`重量(g)`、`长`、`宽`、`高`
六个目标列，只写数值，不改动 B 自带格式与公式。

- 本阶段素材目录：`文件/03/`
- 需求来源：`文件/03/项目需求文档03.docx`（md5 `8e7f51d29fd41a840fcb50f38582be56`）
- 分支：`feature/req03-parallel`，基线 `14ac950 docs: close req02 branch stage`

## 文件身份（按表头实测确定，不按文件名猜）

`文件/03/` 内没有文件名带 B 的素材。按「哪个工作簿含 `导入 单位转换` 工作表」反查：

| 候选 | 含 `导入 单位转换` | 结论 |
|---|---|---|
| `文件/01/B模板01.xlsx`、`文件/01/B模板02.xlsx` | 否 | 排除 |
| `文件/02/B模板.xls` | 否 | 排除 |
| `文件/03/XPY沃尔玛价格计算.xls` | **是** | **B 文件** |

- **A 文件 = `文件/03/A模板02.xls`**（372 数据行，其中 66 行为隐藏行）。
  `文件/03/A模板01.xls` 只有 8 行且 D 列数据区全空，不能作为本阶段源数据。
  两者与 `文件/01/`、`文件/02/` 下的同名文件逐字节相同（md5 一致），非新增素材。
- **B 文件 = `文件/03/XPY沃尔玛价格计算.xls`**（10,533,888 字节，md5 `7422f104cd1332d7ca99ca005f08bf78`）。
- 两个 mp4 是语义补充材料，`*.mp4` 已被 `.gitignore` 忽略，按 T29 登记名称 + 字节数 + md5。

## 已确认需求

1. `A.D 自定义SKU` → `B 导入 单位转换.A SKU(直接从sheet 1导入）`
2. `A.AA SKU价(￥)` → `B.B SKU价(￥)`
3. `A.AE 重量(g)` → `B.C 重量(g)`
4. `A.AY 长` → `B.H 长`
5. `A.AZ 宽` → `B.I 宽`
6. `A.BA 高` → `B.J 高`
7. 定位顺序：先按表头名（字段标题行）匹配；表头名匹配不到时再用需求文档给出的列字母兜底；
   两者都匹配不到则该字段不传输。
8. A、B 均支持路径选择或拖入窗口；按钮选择保留为后备入口。
9. A、B 自带格式（含公式）不得破坏；写入采用「粘贴为数值」语义，无法按数值写入时按文本写入。
10. B 中已有数据时，在最后一条数据行下方空 3 行后继续写入。
11. `自定义SKU` 需兼容真实业务文件中的表头 `自定义`（A模板01 为 `自定义SKU`，A模板02 为 `自定义`）。

### 语义口径（R03-01/R03-02 用户已裁决；R03-03–R03-05 有视频直接证据）

- **R03-01 行准入（用户已裁决 2026-09-12）**：沿用现有一致逻辑——六个字段任一非空即视为一条数据行。
- **R03-02 隐藏行（用户已裁决 2026-09-12）**：保留既有「仅可见行 / 全部数据行」开关，
  本方案默认「仅可见行」。
  视频侧证：实际操作选择的是筛选后的可见单元格，WPS 状态栏显示 `计数=306`；完整源数据为 372 行。
- **R03-03 视频证据**：两段 mp4 已使用 FFmpeg 完整解码并按时间轴抽帧核对，音轨全程静音。
  视频展示了逐列复制、目标处“粘贴为数值”、筛选后 306 行写入以及第二批次追加。
- **R03-04 追加位置**：第二段视频中目标已有数据为第 2-10 行，第 11-13 行留空，第二批从第 14 行开始，
  直接确认“隔三行”采用 `N + 4`。
- **R03-05 值类型**：SKU 列保持文本；`SKU价(￥)`、`重量(g)`、`长`、`宽`、`高` 在粘贴后被
  转为真正数值。视频通知分别显示“已转为数字，共计306处”和三列尺寸“共计918处”。
  **独立复核（Claude，2026-09-12）**：`文件/03/A模板02.xls` 的 D/AA/AE/AY/AZ/BA 六列
  `Value2` **全部返回 `String`**（如 `AA2` 为 `"47"`、`AA373` 为 `"2.04"`、`AY2` 为 `"50"`），
  源数据本身没有数值类型。因此「转为数值」不是可选优化，而是把文本源还原成 Walmart
  导入所需的数值；照搬 Req02 的 `$cell.Value2 = $value` 直通写法会写入文本。

## 技术约束

- 继续使用 Excel COM 打开、写入和保存 B 副本；不得用 openpyxl/ExcelJS 保存 B。
- **末行检测必须只看被映射的目标列。** 现有 `Get-LastRecordRow` 扫描整个 UsedRange 的常量，
  而 B 的 `导入 单位转换` 中 `Q1:Q5220` 是常量 `@100`，`R2:R5220`、`K2:N16601`、`D2:E16601`
  是公式。实测该表 A/B/C/H/I/J 六列第 2 行以下完全为空，但现有实现会得出 5220 并让首批数据
  从第 5224 行写入。Req03 必须改用「只见映射目标列」的末行检测。
- **目标工作表先按名称定位。** `价格` 表的 A1/B1 与 `导入 单位转换` 的 A1/B1 逐字符相同
  （均为 `SKU(直接从sheet 1导入）`、`SKU价(￥)`），仅靠表头会命中两个表。必须以工作表名
  `导入 单位转换` 为主，退回表头判定时须带上 `重量(g)`/`长`/`宽`/`高`，多命中时报错不猜。
- **表头匹配必须精确。** `重量(g)` / `重量(lb)` / `重量(lb)两位小数`、`长` / `长in` 都是不同
  字符串，归一化只做大小写、空白与不换行空格处理，**不得**做全角/半角括号转换或去符号模糊匹配。
  B 表头 `SKU(直接从sheet 1导入）` 本身即半角左括号 + 全角右括号混用。
- Req03 仅支持 `-WriteMode Append`；`Replace` 继续只对 Req02 开放。
- 写入类型须复现视频结果：SKU 写文本；B/C/H/I/J 五个数值字段将可解析的源文本转换为数值后
  写入 `Value2`，无法解析时按原文本写入。转换只接受完整数字字符串，不能截断或模糊转换。
- 字段级跳过：单个字段源或目标匹配不到时计入跳过并继续；B 的 `SKU(直接从sheet 1导入）`
  列匹配不到时整体失败，不静默产出无主键数据。
- 写入区域含公式或合并单元格时停止并提示，不覆盖。
- 表头名称是首要依据；列字母仅作为需求文档明确记录的兜底。
- PowerShell 二维数组下标中的减法必须加括号。
- 主线 01 与支线 02 既有映射、JSON 摘要字段和验证行为保持兼容。
- `ShelfAssistant(settings_path=...)` 与 `_load_paths` 的字符串类型检查必须保留。
- 可选拖放依赖缺失时 GUI 仍须正常启动并可通过「浏览」完成流程。

## 验收标准

- A模板02 的六个字段按表头名写入 B `导入 单位转换` 的 A/B/C/H/I/J 列，逐格比对无错位、无串列。
- 空目标从第 2 行写入；重复运行时从末行 + 4 写入，中间保留 3 个空白间隔行。
- B 的 `Q`/`R`/`K`/`L`/`M`/`D`/`E` 列既有常量与公式逐格不变；`价格` 与
  `运费表（公式数据，不动）` 两个工作表不得被写入。
- 原始 B 文件 `XPY沃尔玛价格计算.xls` 的 md5 保持不变
  （`7422f104cd1332d7ca99ca005f08bf78`）；输出始终是 B 的副本。
- 六条映射任一条断链时给出可读错误或计入 `skipped`，不猜测相邻列、不静默写入。
- 目标列缺失或歧义时停止；`价格` 表同名字段不得被当作目标写入。
- **R03-06 输出格式（用户已裁决 2026-09-12）**：输出必须沿用 B 的 `.xls` 格式；GUI 与映射器
  均拒绝其他输出扩展名，避免格式转换和扩展名与实际二进制格式不一致的风险。
- 主线 01 与支线 02 不回归：既有 B01 逐格验证、支线 02 验证、追加验证、对齐验证、
  Python unittest 全部通过，结果写入 `HANDOFF.md`。
- 界面可明确选择「支线 03」；三个方案的路径与导出范围互不覆盖，重启后恢复最后页面与各组设置。

## 分工

- Codex（主负责人）：实现 Req03 profile、GUI 第三方案页、测试、修复、整合与提交。
- Claude（独立审查）：需求/验收审查、边界与回归检查、文档与使用流程检查；
  接口冲突先记入 `HANDOFF.md`。

## 当前状态

- 分支：`feature/req03-parallel`，基线 `14ac950`，已创建并切换（2026-09-12）。
- 三份共享文档、Req03 映射器、第三方案 GUI、专项测试和交付文档均已完成（T23–T29）。
  Claude T28 首轮发现的非 `.xls` 输出缺陷已修复，针对性复审通过；最终回归、构建、安装和启动检查通过。
- R03-01 行准入、R03-02 默认导出范围和 R03-06 默认 `.xls` 输出均已由用户裁决；
  R03-03 视频内容、R03-04 `N+4`、R03-05 数值类型均有视频直接证据
  （R03-05 另经源文件类型实测复核）。当前没有待确认的语义项。
- 素材基线已登记：B 文件、docx、A模板01/02 的 md5 见上文与 `HANDOFF.md`。
