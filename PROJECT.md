# 项目需求文档 02 并行功能

## 目标

在不修改主线既有 B01 模板行为的前提下，为 `文件/A模板02.xls` 与 `文件/B模板02.xlsx` 增加可验证的映射流程，并在 Tkinter 界面中支持导入路径或拖入文件。输出必须是 B 模板副本，只写入值，保留模板格式、公式和结构。

## 已确认需求

1. A 文件指定字段按表头名称优先映射到 B 文件的字段或多级表头字段；无法确定目标时跳过并记录。
2. `A.SKU -> B.SKU`。
3. `A.自定义 -> B.平台SKU`（目标表头目前在 B02 实测未找到，见待决事项）。
4. A/B 支持路径选择或拖入窗口。
5. 使用 Excel COM 保存 B 副本；不得用 openpyxl/ExcelJS 保存 B。

## 技术约束

- 原始模板不得改写；映射器继续使用临时副本和 Excel COM。
- 表头名称是依据，列字母只可作为明确记录的兜底，不得默认相信需求文档列字母。
- PowerShell 脚本新增内容须 UTF-8 BOM；保留现有空值、二维数组下标和 COM 清理约束。
- B01 现有映射和全部既有验证不得回归。
- `ShelfAssistant(settings_path=...)` 与配置类型检查必须保持可注入、可测试。

## 验收标准

- 能在新分支运行 B02 映射，生成独立输出副本，原 B02 模板哈希/结构不变。
- SKU 映射在合成夹具和真实 B02 模板上逐格正确；目标缺失字段不会误写相邻列，并在摘要中可见。
- GUI 可选择 A/B/输出路径；拖放能力若受运行环境限制，至少有明确的可测试入口和错误反馈。
- B01 原验证、Python unittest、B02 专用验证、OOXML/格式保护验证均有记录。
- 未决的“平台SKU”目标在人工确认前保持阻塞，不以猜测实现。

## 分工

- Codex（主负责人）：代码实现、接口设计、测试运行、问题修复、最终整合与提交。
- Claude（独立审查）：需求整理、验收标准、方案遗漏/边界审查、回归检查、文档与使用流程检查。若需修改对方负责模块，先在 `HANDOFF.md` 记录协调事项。

## 当前状态

- 分支：`feature/req02-parallel`
- 基线：`68c2ad39 chore: 入库第二套模板 A/B模板02`
- 关键阻塞：B02 可见目标表 `Product Content And Site Exp` 未发现“平台SKU”表头，需人工确认其真实目标或批准显式别名策略。
